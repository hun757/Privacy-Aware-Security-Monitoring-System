# Copy this file to ".env" and fill in real values.
# ".env" itself must NOT be committed to GitHub (add it to .gitignore).

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=여기에_실제_비밀번호
DB_NAME=privacy_db
